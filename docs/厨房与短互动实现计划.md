# 厨房与短互动实现计划

状态：1.8.0 已按反馈移除“研究搭配”，保留做饭；改动已推送 main，全量构建与 Release 草稿附件已同步更新。

## 1.8.0 移除研究搭配（2026-09-11）

- 目标／授权：用户反馈食谱功能无用，已明确仅去掉“研究搭配”，保留做饭。沿用前轮主分支推送、全量打包和 Release 草稿准备授权，同步替换尚未公开的 1.8.0 草稿包。
- 决定：移除厨房研究页签、选料与配方匹配界面、无调用的匹配函数及样式；厨房保留食谱本、厨具与摆盘、试吃留言，食材说明改为仅用于制作。制作、配方、库存、首做经验和记忆存档无需迁移。
- 完成／验证（2026-09-12）：正式源码无研究页面、匹配函数或专用样式残留；check:companion-activities、build 和 git diff --check 通过，制作、食材、库存、记忆及中英文静态渲染正常。提交 8413dea8b8779e8fb7244ade31ae268acfadf3c6 已推送 main；全量构建 https://github.com/T-meow/PocPet/actions/runs/34617795058 全部成功，版本／存档／玩法／数值／双版本前端及全部原生平台检查通过。
- 交付：release/ 下 8 个 pocket1.8.0* 文件已更新，总计 292,508,371 字节；完整新版及 update-info.json、SHA256SUMS.txt 在 release/1.8.0-8413dea/。已核对本地架构、Web revision、Android 1.8.0／10800、固定签名及 v2／v3 校验。旧目录 release/1.8.0 在归集时已不存在，本轮未删除或重建；更新草稿前按上一轮记录的 SHA-256 确认原有 10 个附件未被外部更改。
- Release：同一 v1.8.0 草稿（ID 387129619）已更新目标至 8413dea8，发布说明与 10 个新版附件齐全；所有 GitHub 附件大小／SHA-256 与本地一致。当前链接 https://github.com/T-meow/PocPet/releases/tag/untagged-b6507c4e7016f41c45fa ，已确认仍为草稿，未公开或部署 Pages。本轮代码、推送和草稿包更新已完成，待用户自测及选择公开时间。
- 路径／禁动：KitchenModal.tsx、kitchenRecipes.ts、items.ts、companion-activities.css、既有检查及发布记录；不修改原型、历史玩家存档、已有 1.7.2 测试包，不公开 Release。

## 1.8.0 合并与全量发布准备（2026-09-11）

- 目标／授权：用户要求合并主分支、推送 GitHub、全量打包并准备 Release。授权提交当前版本源码、资源、原型和已有文档整理，合并 main 并推送，调用现有 GitHub Actions 全量构建，下载校验交付包并创建 Release 草稿。
- 决定：dev 与 origin/main 同基线 fd75e905，可快进合并。现有标签推送流程会自动公开 Release 并部署 Pages；本次使用 workflow_dispatch full_build=true 构建目标提交，准备草稿及说明／附件。沿用固定 Android 测试签名；全量包含 Windows x64／x86、Android arm64／ARMv7、Web、macOS、Linux AppImage／deb 共 8 包及客户端更新清单。
- 完成：dev 提交 d427daedbfe80544193c15e5c3071ab2318d6abf 已快进合并 main 并推送 origin/main。全量构建 https://github.com/T-meow/PocPet/actions/runs/34613429361 成功，版本／存档／玩法／数值／更新检查、标准版和 Bilibili 版前端以及全部原生平台通过；发布说明见 docs/1.8.0-release.md。产物固定对应 d427daed，后续仅补交本文和 macOS 架构说明，不改构建源码。
- 交付：release/ 下已按规则保存 8 个 pocket1.8.0* 平台包，总计 292,516,993 字节（292.5 MB）；完整交付集合在 release/1.8.0/，附 update-info.json 和 SHA256SUMS.txt。Windows 为 x64／x86，Android 为 arm64／ARMv7，macOS 为 Apple Silicon arm64，Linux 为 amd64 AppImage／deb，Web 为标准版部署 ZIP。
- 验证：完整 8 包集合校验通过，CI 验证原生架构与前端资源版本，下载后复核 Windows／APK 架构、Web build-info 版本与 revision。两个 APK 均 versionName=1.8.0、versionCode=10800，签名 SHA-256=E375653D29A6738BC45B1EF34B6B1B6BD86DDA66C53D751DDEE3683ACECCD285，v2／v3 验证通过，与各自固定签名基线一致；更新清单含 6 个客户端目标。
- Release：已创建 v1.8.0 草稿，https://github.com/T-meow/PocPet/releases/tag/untagged-70f25f14cd1ec51867cc （ID 387129619），说明和 10 个附件齐全，GitHub 记录的每个文件大小与 SHA-256 均与本地一致。已确认 draft=true、published_at=null、目标提交与 CI 相同；尚未公开发布或触发 Pages 部署。
- 待办：用户自行进行真机覆盖安装／游玩测试，并选择草稿公开时间；本次合并、推送、全量打包和发布准备已完成。
- 路径／禁动：.github/workflows/release.yml、docs/1.8.0-release.md、既有打包脚本与 release/。保留旧 APK／测试存档及已有文档删除状态；不读取或更换签名凭据，不操作玩家数据，无 Computer Use／多代理。

## 1.8.0 存档兼容与版本同步（2026-09-11）

- 目标／授权：用户要求检查存档兼容性，准备发布测试，版本改为 1.8.0。先检查并修复兼容问题、同步元数据、完成必要回归；本轮不发布或触发远程构建。后续打包 1.8.0 按 AGENTS.md 全量规则执行，macOS／Linux 由对应系统或 CI 构建。
- 决定：沿用 PocPet applicationId、存档主键 pocpet.pet.v1、文件 schemaVersion=1 和 POCPET-SAVE-v2 保护格式；保持食谱／物品／角色 ID。旧档首次载入保留版本独立的升级前原始副本；日程 v5→v6 只提速一次；小游戏读档暂停且不重复扣球／发奖。
- 完成：package、lock、Tauri、Cargo、前端 fallback 均为 1.8.0，Android versionCode=10800。修复厨具 equipment、小游戏 unlocked 非数组时调用 includes 导致整份存档被判损坏的问题：异常可选列表回退默认值，合法库存、玩法记录及其他字段继续读取。
- 兼容验证：补充旧版缺少厨房／小游戏／记忆字段的原始档、JSON 信封档及保护格式往返；确认旧料理 ID、Mod 物品、库存、首做／初尝记录和角色记忆保留。三个未完成小游戏读档暂停、离线不算参与时间、不重复扣球，按新等级曲线结算；已经完成的旧奖励保留原值，不追扣、不重复发放。12 个日程均验证 v5→v6 只缩短一次、导入保留剩余时间、已过期旧日程的结束时间保留、奖励快照与领取幂等。
- 备份验证：首次升级生成独立的升级前原始副本，1.7.2 和更早的升级备份仍可检索且原字节保留；备份失败阻止覆盖主档，恢复后可重试。主档／备份角色身份、导入失败回滚、跨页面旧状态写入拦截均通过。
- 样本／检查：既有 Mint 1.0.1 保护存档通过；release/pocket1.7.2-test.pocpet 只读导入及导出后重导入通过（Lv.20、金币／心心各 1,000,000、51 种库存；玩法字段及资产稳定，源文件哈希未变）。check:save-recovery、check:mint-compat、check:companion-activities、check:partner-schedule、check:offline-lifecycle、check:time-guard、check:sleep-settlement、check:release 和 build 均通过，保留原有 Vite 混合导入与包体提示。
- 边界／待办：上述为现有历史样本及构造旧字段的自动化验证；Android 真机覆盖安装后的读档／导入体验尚待用户测试。本轮未生成 1.8.0 安装包或发布，release/pocket1.7.2.apk 仍是上一版测试包。
- 路径／禁动：src/core/saveCodec.ts、petState.ts、kitchen.ts、miniGames.ts、partnerSchedule.ts、storage.ts 及既有 scripts/check-*；保留用户文档删除状态和其他未提交改动，不修改真实玩家存档、现有 1.7.2 APK 或测试存档，不提交、不调用 Computer Use。

## Android 测试包交付（2026-09-11）

- 授权／目标：用户要求打包 APK 自测。按项目规则仅构建 Android arm64，复用既有 debug keystore，版本 1.7.2，输出 release/pocket1.7.2.apk。
- 完成／验证：package／Tauri／Cargo 均为 1.7.2；package:android:arm64 已完成前端及 arm64 原生编译（原生库时间 2026-09-11 20:55:04），随后受 Windows 符号链接权限限制。接着运行项目既有 package:android:arm64:reuse，校验刚生成的原生库包含当前前端（fd75e905），复制入 Android 工程完成 Gradle 打包，无需系统设置变更。APK 架构、资源、applicationId=com.frostforge.pocpet、versionName=1.7.2、versionCode=10702 及 v2／v3 签名校验均通过；签名与项目 aarch64 基线一致。
- 交付：D:/Projects/PocPet/release/pocket1.7.2.apk，34,117,187 字节（34.1 MB），SHA-256：D6A8B2F47ADF98B0931CF60EBFC4D55996F25ED696565CEB0BEF8ABB8402DDA7。用户自行在 Android 手机验收，未安装或操作设备。
- 路径／禁动：scripts/package-android-arm64-debugsigned.ps1 与现有 Android 工程；保留用户其他改动和已删除文件状态，不提交、不发布、不改玩家存档，不生成其他平台交付包。

## 草莓牛奶甜点配方（2026-09-11）

- 授权／决定：部分甜点改用现有 strawberry_milk。奶香饼干杯改为草莓饼干杯（苏打饼干 ×1＋草莓牛奶 ×1）；奶香小饼干改为草莓小饼干（面粉 ×1＋草莓牛奶 ×1）；苏打饼干千层改为草莓饼干千层（苏打饼干 ×20＋草莓牛奶 ×2＋鸡蛋 ×1）。同步中英文菜名，原食谱及成品 ID、食用效果、制作收益保留。
- 完成／验证：商店和背包食材页按配方自动纳入草莓牛奶，食材共 11 种，共用原库存和物品图。check:companion-activities 与 build 通过，已覆盖批量扣料、配方研究、双分类和静态渲染；仅保留原有构建提示。版本 1.7.2，不改玩家存档和其他任务改动。
- 路径：src/core/kitchenRecipes.ts、scripts/check-companion-activities.ts。

## 快捷玩耍恢复与游戏入口命名（2026-09-11）

- 授权／决定：恢复清洁旁的快捷玩耍，直接调用原有 onAction('play') 体力换心情操作；保留低体力、过饿和日程忙碌限制，不受小游戏 Lv.3 门槛影响。主页入口改名“一起游戏／继续游戏”，小游戏大厅标题及关闭标签同步。
- 完成／验证：日常照顾栏为背包／喂食、玩耍、清洁、睡觉四个浅色按钮，窄屏图标文字上下排列。check:companion-activities 与 build 通过；原有构建体积与混合导入提示保留。
- 路径／禁动：HomePageV2.tsx、PlayModal.tsx、companion-activities.css；版本 1.7.2，保留其他改动及存档，无 Computer Use、提交或打包。

## 制作弹窗、料理收益与日程提速（2026-09-11）

- 目标／授权：制作过程独立弹窗，点击有对应音效和操作动画；可从本地素材库补音频。减少料理心心，料理技能增加心心收益。用户追加：每个伙伴日程时长至少减半。
- 决定：制作弹窗替换食谱弹窗展示，三步主动操作（投料、按厨具加工、装盘），结束才扣材料并发放食物／心心；中途返回不扣材料。不同厨具分别拌制、翻炒、搅打、烘烤，结果留在独立弹窗，可查看最近出炉、喂一份或回食谱。每份基础心心 round(3 × 角色状态倍率)，料理技能每级 +10%（Lv.1 为 0%，Lv.10 为 +90%，心心取整），已有产心加成另算；批量按份应用加成，首做经验规则保留。
- 日程决定：四类伙伴日程的短／标准／长基础时长从 45／120／240 分钟降为 20／60／120，技能加速继续叠加。升级日程 schema，使已有未结束日程总时长按对应比例缩短一次，已耗时间照常计入；达到新时长则正常转为待领取。收益、消耗、每日次数及专注计时保持现有规则。
- 追加决定：小游戏不加每日收益次数限制，按用户新锚点使用 round(5 + 145 × ((角色等级 − 3) / 96)^1.65)，Lv.3／10／20／50／99 为 5／7／13／50／150 心，接球仍三倍并每局扣一个球。Lv.3 解锁、快节奏及原有加成保留。
- 追加菜谱：白米饭＝大米 ×1，饱腹 +30，原料 12 金币，仅食用效果无其他属性；用户确认制作仍给普通料理的心心及首做经验。苏打饼干千层＝苏打饼干 ×20、牛奶 ×2、鸡蛋 ×1，使用烤箱，饱腹 +80／心情 +30／体力 +25／健康 +10；支持不同用料数量及批量扣料，总计 14 食谱／16 成品，新增图片继续占位。白米饭使用加水焖煮动作。
- 进度：已完成核心、独立制作／结果弹窗、四厨具及焖煮动画音效、日程迁移、新配方与小游戏曲线。日程料理技能卡补充当前制作心心加成。已从 D:/游戏素材/sfx music 裁剪 3 个 MP3，合计 26,070 字节，源路径与 SHA-256 在 src/assets/audio/kitchen/SOURCES.md；原素材未修改。
- 验证：check:companion-activities、check:partner-schedule、check:offline-lifecycle、check:save-recovery、check:stat-scaling-batch 与 build 通过，已跟踪文件 git diff --check 通过。覆盖料理技能等级／角色等级、批量和首次升级快照、重复结算／旧结果、不同用料数量与不足时不扣料、14 食谱／16 成品、操作状态及音效映射、独立弹窗中英文静态渲染、小游戏新收益、12 个日程在不同技能级的时长／单次迁移／待领取与重复领取。日程旧检查同步到厨房上线时已固定的任务达人成就池；构建保留原有体积与混合导入提示。
- 预览／待用户体验：127.0.0.1:5173 正常提供最新代码，主页、厨房／制作弹窗、新食谱、小游戏、日程、样式及三个 MP3 均 HTTP 200。真实触屏排版、动画手感和音量待用户体验；本轮未用 Computer Use。未修改真实玩家存档或测试存档，旧进行中日程在游戏正常读档时自动迁移。
- 路径／禁动：src/core/kitchen.ts、companionActivityTypes.ts、partnerSchedule.ts、audio.ts、src/ui/KitchenModal.tsx、新制作弹窗与样式及相关检查。保留其他任务改动、原型、真实玩家存档、测试存档，单助手，无 Computer Use，不打包或提交；版本 1.7.2。

### 四技能作用复核与建议（讨论项）

- 当前共通效果：各自日程 Lv.2 体力消耗 −10%，Lv.4 饱腹／心情消耗 −10%，Lv.5 时长 −5%，Lv.7 经验 +15%，Lv.8 金币 +5%，Lv.9 时长 −10%。四项都达到 Lv.3／6 后，日程选择数 4／5、全局日程金币 +5%／15%；每日完成上限仍为 3。每个技能升满需要 2,220 经验。大师日程累计 10／30／60 次继续增强。
- 学习：现有 Lv.10 每日小愿望金币 +10%，大师 60 次后 +20%。建议把普通加成按 Lv.3／6／10 分为 +3%／6%／10%，以后承接冒险线索或图鉴发现，不再直接放大小游戏心心。
- 料理：本轮已增加每升一级制作心心 +10%，最高 +90%。现有 Lv.10 食用正向效果 +10%，大师 60 次后 +15% 保留；每种料理首做给 5 经验，重复与批量制作不刷经验。
- 园艺：现有 Lv.10 种植时间 −5%，大师 60 次后 −8%。建议按 Lv.3／6／10 提前给 −1%／3%／5%，以后主要关联植物图鉴和特色食材，避免继续叠金币或心心乘区。
- 运动：现有 Lv.10 清醒体力恢复间隔 −8%，大师 60 次后 −12%，睡眠恢复不受影响。建议按 Lv.3／6／10 提前给 −2%／5%／8%，以后用于冒险行动效率；本轮按用户选择不把它做成小游戏每日有收益次数门槛。
- 上述学习／园艺／运动的分阶段跨玩法加成为建议，尚未改动数值。角色等级驱动小游戏心心，料理技能驱动做饭心心，各技能负责不同玩法的效率，避免四项都变成重复产心技能。一年满级仍需把未来冒险、金币流入与额外加成合并测算，不能用单局收益承诺固定天数。

## 主页配色与商店／背包 v2（2026-09-11）

- 目标／授权：用户确认完整计划并要求实施。主页六入口常驻浅色；接球复用现有玩具球图片；商店、背包改为共用格子＋详情弹窗，加入搜索及食材分类，版本保持 1.7.2。
- 决定：全部／食物／食材／物品／护理／园艺为界面分类，不改物品 kind 与 Mod 协议。菜谱用料和 kitchen_material 标签自动归集；专用原料仅归食材，可食用用料在食物与食材共用库存。宽屏左右布局、手机格子上方＋底部详情操作。交易继续调用现有报价、限额与批量规则。
- 进度：已完成。六入口分别使用杏橙／粉／紫／金／蓝／绿；接球入口、待抛与飞行图片均使用当前 toy_ball 映射并回退内置图。商店与背包共用目录、搜索、分类、详情及浏览状态，厨房补货默认食材页，商店／背包互切保留返回厨房或小游戏的上下文。已保留其他任务改动及未跟踪原型。
- 验证：check:companion-activities、check:stat-scaling-batch 与 build 均通过；相关已跟踪文件 git diff --check 通过。覆盖十种菜谱用料、专用原料分离、双分类同库存、去重、Mod 标签与搜索、数量自动校正、耗尽与空态、Lv.20 批量和特殊物品、实际优惠报价／免费领取、补货返回、真实球图及中英文静态渲染。原有构建体积和混合导入提示保留。
- 预览／待用户体验：原开发服务已停止，使用项目 scripts/serve-dev.mjs 在后台重新启动（PID 31148，127.0.0.1:5173）。主页、新目录、商店、背包、样式及接球模块均 HTTP 200；手机真实排版与触控仍待体验。
- 路径／禁动：新增 src/ui/ItemStorageModal.tsx、src/ui/itemBrowse.ts、src/styles/item-storage.css；调整 ShopModal、InventoryModal、App、useInventoryController、主页与接球图片样式。未修改原型、玩家存档、测试存档及版本；未增加依赖、使用 Computer Use、打包或提交。

## 小游戏等级门槛与收益调整（2026-09-11）

- 授权：小游戏至少 Lv.3 解锁；初始收益低，随等级加成提高，避免前期升级过快、后期收益不足。
- 决定：三个小游戏统一 Lv.3 开放。翻牌／泡泡基础心心为 `round(8 + (等级 - 3)² / 4)`，Lv.3／10／20／50／99 为 8／20／80／560／2312；接球仍为三倍且每局消耗一个玩具球，已有产心加成另计。升级成本不变。
- 兼容：低等级旧对局暂停并保留进度，Lv.3 后可以继续，不重复扣球；旧局的奖励等级最低按 Lv.3 迁移。已完成的收获不重算，新局继续锁定开局等级。
- 进度：已完成主页锁定提示、弹窗防护、开始／恢复／动作校验和收益调整。
- 验证：`npm.cmd run check:companion-activities` 与 `npm.cmd run build` 通过。覆盖 Lv.1–2 拥有道具仍不可开局／扣费、Lv.3 正常开放、低等级旧局暂停与升级后恢复、多等级实际结算、接球三倍收益、主页和弹窗锁定渲染；厨房、音效事件、保存与重复结算回归通过。本地玩法与主页模块 HTTP 200，已提供新规则。保留既有构建体积与混合导入提示；实际游玩节奏待用户体验。
- 路径／禁动：`src/core/miniGames.ts`、`src/ui/HomePageV2.tsx`、`src/ui/PlayModal.tsx`、`src/ui/app/useCompanionActivities.ts`、`scripts/check-companion-activities.ts`。版本保持 1.7.2；保留现有其他任务改动，不修改玩家存档或测试存档，不打包或提交。

## 小游戏反馈与结算（2026-09-11）

- 授权：给三个小游戏接入音效和动效，完成后弹窗展示本次收获，并提高等级收益。
- 决定：翻牌／泡泡每局基础心心为 `80 + 8 × (等级 - 1)`，Lv.1／20／50／99 分别为 80／232／472／864；接球为其三倍。模式、成绩不影响基础奖励，已有产心加成另计。新局按开始等级锁定，旧进行中局按当前等级迁移；厨房收益保持现状。
- 用户补充已实施：接球每局开局扣 1 个玩具球，共十次主动滑动抛球；宠物左右移动，单次飞行 450ms，按落点判定接住。暂停恢复不再扣球，再开一局才扣下一球；不够球时直接去商店。吹泡泡长按上限改为 1.5 秒，三颗泡泡且至少六秒有效互动／前台时间即可结算。
- 反馈：复用现有音效资源与声音开关，按成功提交的动作播放；补充翻牌、配对、抛接、泡泡生成／戳破和结算动效，尊重减少动态效果设置。
- 结算：奖励在完成时到账；弹窗显示实际心心、心情变化和成绩，可返回列表或再来一局。待查看结果随存档保留，关闭／重开不重复发奖。
- 进度：已完成音效、动效、新操作、等级收益与独立结算弹窗；滑动抛球之外也支持方向键瞄准、空格抛球。
- 验证：`check:companion-activities` 与 `build` 通过，相关已跟踪文件 `git diff --check` 通过。回归覆盖十次抛球只扣一球、缺球／重复开始／暂停恢复、落点命中与未命中、六秒泡泡、等级收益及开局等级锁定、旧局迁移、结算保存与重复确认不发奖、无效点击／刷新不重复触发音效，以及玩法和结算弹窗静态渲染。本地开发模块返回 HTTP 200。
- 待用户体验：真实触屏滑动手感、音量与画面效果；本轮未做 Computer Use 视觉或听觉验收。复用既有音频素材，没有新增外部依赖、修改测试存档、打包或提交。主要入口为 `src/ui/play/CatchBoard.tsx`、`MiniGameResultModal.tsx`、`useMiniGameFeedback.ts` 与 `src/core/miniGames.ts`。

## 小游戏启动／点击退回修复（2026-09-11）

- 授权：排查并修复小游戏点开后闪退，保留当前主页、收益和存档。
- 已复现：`startMiniGame` 后为运行态，但 `advancePet` 的每秒刷新以及点击前刷新都会调用默认读档规范化，令 `paused` 变为 `true`，牌面点击被忽略并返回列表。此前独立动作与静态渲染检查没有覆盖这条组合路径。
- 处理：已修复。运行中的数据规范化保留小游戏运行状态、计时及抛球／吹泡泡时间；接球反馈也随刷新保留。保存副本默认暂停，读档、导入与时间回拨继续暂停；关窗／切后台行为不变。
- 验证：新增三游戏的实际刷新／点击串联回归，覆盖亚秒刷新、每秒刷新、翻牌配对、抛接、吹泡泡、暂停恢复、原始存档加载、离线不计时与时间回拨。`check:companion-activities`、`check:offline-lifecycle`、`check:save-recovery`、`build` 均通过，相关 `git diff --check` 通过。本地 Vite 已提供更新后的模块，HTTP 200。
- 路径：`src/core/miniGames.ts`、`src/core/petState.ts`、`src/core/petLifecycle.ts`、`src/core/saveCodec.ts`、`scripts/check-companion-activities.ts`。不修改玩家本地存档，不调用 Computer Use，不打包或提交。

## 主页交互调整（2026-09-11）

- 授权：更新一版可体验的主页，将常用功能移出“所有事情”菜单，纪念册只展示经历；为后续冒险预留入口接口并锁定。
- 决定：做饭／玩耍并列主入口；花园／日程／专注／打工为活动入口；商店／搭子卡／扭蛋与成就／梦想／纪念册常驻主页；设置回到顶栏。保留厨房补货返回和小游戏暂停恢复。
- 冒险：使用显式锁定状态与未来打开回调的类型接口，当前没有可进入的冒险页面，不添加虚假等级门槛、地图进度、收益或存档字段。
- 进度：已移除“所有事情”菜单和纪念册玩法按钮，完成主页分组、独立设置入口和冒险锁定卡；小愿望移到主页。缩小角色在房间里的尺寸，手机布局依次为角色／玩法与常用入口／今日小事。未完成的同角色小游戏从主页入口直接恢复，忙碌或睡眠时保持暂停。
- 冒险接入点：`src/ui/AdventureEntry.tsx` 导出 `AdventureEntryState`（`locked`／`available + onOpen`）；`src/ui/App.tsx` 显式传入 `locked`。锁定时按钮禁用且没有点击回调，未来提供真实目的地后再切换状态。
- 验证：`npm.cmd run check:companion-activities` 与 `npm.cmd run build` 通过；入口相关已跟踪文件 `git diff --check` 通过。保留既有构建体积和混合导入提示。仅调整现有渲染检查的主页参数，未增加临时测试工程。
- 本地预览：原开发进程已停止，现已重新在后台启动 Vite（`127.0.0.1:5173`），HTTP 200，并打开预览。待用户验收实际窗口排版与触控；没有进行 Computer Use 视觉验收。
- 路径／禁动项：主要修改 `src/ui/HomePageV2.tsx`、`src/ui/App.tsx`、`src/ui/app/useAppNavigation.ts`、`src/styles/companion-activities.css`；保留其他任务的存档、翻译、Android 和未跟踪原型。版本继续为 1.7.2；不打包、不发布、不代提交，不调用 Computer Use 或生成美术。

## 1.7.2 已确认实施安排

- 新版 UI 本轮覆盖小窝主页和新增弹窗；厨房与玩耍使用主界面弹窗，不增加独立页面。已有其他功能继续使用真实数据与原有业务逻辑。
- 完成十二道基础配方、四种做法、翻牌／接球／吹泡泡以及三条记忆后续；先打通完整流程，再扩齐内容。
- 每份料理、每局有效互动持续给予 20 基础心，再应用已有等级和产心加成。无每日次数上限与递减，辅助模式同等奖励；约一年满级作为参考，不作为限制进度的目标。
- 食材与厨具使用金币；制作没有额外加工费，批量按份结算。首入厨房赠送两道教学菜的试做材料；基础翻牌免费开放，旧玩具球解锁接球，泡泡棒永久购买。
- 采用可替换的道具占位图与已有角色资源，本轮不调用 AI 作图或电脑操作；以后按资源清单替换美术。
- 开发顺序：核心状态与物品 → 厨房与三类互动 → 记忆及成就 → 新版主页和弹窗接入 → 1.7.2 版本同步与必要数据／构建检查。
- 保留当前工作区已有的存档下载、设置、翻译和 Android 改动；本轮不自动提交或发布。

### 进度与验证（2026-09-11）

- 已接入新版小窝主页、常驻厨房／玩耍入口、更多功能与纪念册；两个玩法共用主界面弹窗。厨房跳转商店后返回原配方和数量，小游戏关窗、切后台或伙伴忙碌时暂停。
- 厨房已包含四种基础材料、十二道配方与十四种成品（松饼、布丁各有苹果／香蕉口味）、四种做法、批量制作、料理首做经验和实际喂食留言。开局试做材料只送一次。
- 三种小游戏均已可玩并结算：翻牌六对、接球十轮、吹泡泡至少五个且有三十秒有效参与／前台时间；辅助模式同奖励。记录与玩法外观可保存，三条记忆后续可完成。
- 已接入六项厨房成就和三项小游戏初次完成成就；固定旧物品收集清单，新成就不扩大旧“任务大师”目标。料理首做经验不增加日程完成或大师次数。
- 已增加旧档默认值、物品注册、角色身份记录、游戏暂停恢复和导入时间处理；原有存档下载、设置、翻译及 Android 工作区改动继续保留。
- `npm.cmd run check:companion-activities` 通过：材料与数量校验、批量／逐份结算、一次性奖励、十四种成品、记忆归属与后续、三游戏完成／暂停／重复结算、旧档与备份导入，以及真实 React 组件的静态渲染。
- `npm.cmd run build` 与 `npm.cmd run check:release` 通过，版本为 1.7.2。构建有 Vite 包体积及混合导入提示，无编译错误。未执行 Computer Use；手机实机触控、窗口尺寸与最终美术效果留待用户验收。
- 本轮不生成安装包、不发布、不提交 Git。验收后建议将新增源码、检查脚本和本记录一起提交；已有未跟踪原型按用户意愿留档。

后续美术替换入口为 `src/companionActivityAssets.ts`（四种食材、十四种料理的稳定 ID，占位图 128×128），以及厨房／玩耍组件中的场景、厨具、牌面、球、泡泡和纪念卡。角色反馈继续走当前角色资源表，缺少动作时使用该角色的基础图，不混用其他角色。

以下为前期设计记录，涉及页面形式和候选收益的旧表述以本节为准。

## 本轮计划

1. 将冒险地图目录补全为五个，明确各自特色产物、基地与图鉴方向。
2. 核对当前页面入口、物品注册、角色身份、状态存储和成就结算接入点。
3. 列出厨房与三类短互动的首发流程、内容配置和必要存档字段，先做能独立完成的体验。
4. 将基础记忆、收益配置、兼容处理和开发顺序落到具体模块；数值与批量制作规则继续明确标注为候选。
5. 给出少量关键验收项，读回中文与交叉引用；不提前实现冒险或运行无关构建。

## 实现目标与范围

玩家可以直接进入厨房研究或制作料理，喂给伙伴并留下试吃记忆；也可以随时进入玩耍页，与伙伴接球、翻牌或吹泡泡。两个系统各自拥有持续玩法、收藏与成长反馈，在冒险上线前即可完整使用。

本计划采用 12 道基础配方、四种做法、三类短互动和三条基础记忆链作为实现范围。角色覆盖 Furo、Doro、mint，所有基础材料有商店或既有花园来源。菜单数量属于可实施的内容安排，食物数值、重复产心规则与具体对白继续调整。五地图作为后续冒险范围，详见 [玩法扩展讨论草案第十四节](玩法扩展讨论草案.md#十四基地访问地图产出与图鉴)。

## 已核对的代码接入点

| 现有位置 | 本次需要接入的内容 |
| --- | --- |
| `src/ui/app/useAppNavigation.ts`、`src/ui/App.tsx` | 增加厨房与玩耍页，组装控制器和角色上下文 |
| `src/ui/HomePage.tsx`、`src/ui/FeatureRow.tsx`、`src/ui/ActionDock.tsx` | 提供厨房、短互动常驻入口，保留旧的直接玩耍恢复操作 |
| `src/core/items.ts`、`src/core/petTypes.ts` | 增加食材与成品 ID、物品定义，接现有背包和数量逻辑 |
| `src/core/petActions.ts` | 成品喂食、首次试吃记录和玩具的永久玩法解锁 |
| `src/core/petState.ts` | 新建默认状态、读取旧档时补字段、校验持久化进度 |
| `src/core/achievements.ts`、`src/core/partnerSchedule.ts` | 制作与游玩计数、料理首次经验、旧目标兼容 |
| `src/ui/app/usePetSession.ts`、`src/core/storage.ts` | 继续使用现有 `setPet`、`commitPet` 和保存路径 |
| `src/core/saveCodec.ts`、`src/core/gameClock.ts` | 新字段随备份导入导出，活动时间在导入与时间回拨时正确处理 |

当前页面状态只有主页、成就、花园、日程与伙伴梦想；物品注册已经支持可使用标记、Mod 覆盖与未知物品回退。`commitPet` 负责成就评估，实际保存由会话的状态变化触发。新逻辑返回一次完整的新 `PetState`，不在页面各处分别写库存、奖励和进度。

## 厨房流程

厨房页提供食谱本、研究搭配与厨具入口，主区域展示伙伴和制作台。食谱详情明确列出材料需求、成品作用、制作份数和本次可得心心；缺少材料时可以打开现有商店补齐。

- 已知食谱：选配方和数量 -> 预览 -> 确认制作 -> 制作演出或可选操作 -> 查看成品 -> 收入背包或喂食。
- 研究：选 1-3 种食材与做法 -> 查看线索与有效组合 -> 确认制作 -> 记录发现。无对应配方时保留食材。
- 厨具：拌制和平底锅起步可用；制作若干不同基础菜后，可用金币购买搅拌机与烤箱。条件与价格放在配置表。
- 试吃：通过现有喂食路径消费成品，再记录该角色第一次吃过这道菜的反应；收藏留言可以在食谱详情回看。

制作确认时校验配方、厨具、材料、数量和伙伴状态，一次扣除成本并生成成品、心心与制作记录，保存后播放演出。退出演出保留已经制作好的料理，可选搅拌、火候和摆盘只补充表现或作品，不再次发奖。这样直接制作与手动参与具有相同基础收益，也无需增加实时烹饪队列。

批量数量必须是正整数，并受实际材料约束。研究输入按明确的食材 ID、数量和做法匹配，标签只提供提示；不能因为 Mod 改了食材名字就匹配到另一道菜。同一次制作请求重复提交，只使用一次操作标识结算。

### 十二道基础配方安排

新增基础食材先使用大米、鸡蛋、面粉、胡萝卜。米饭等必要的基础预处理包含在制作演出中，不额外增加半成品合成链。AD 钙奶、现有水果和苏打饼干沿用原物品身份，显示名可以随角色外观变化。

| 配方 | 材料安排 | 做法 | 主要用途 |
| --- | --- | --- | --- |
| 双果沙拉 | 苹果 + 橙子 | 拌制 | 轻食与心情恢复，教学配方 |
| 香蕉奶昔 | 香蕉 + AD 钙奶 | 搅拌机 | 体力与饱腹组合 |
| 西瓜冰饮 | 西瓜 | 搅拌机 | 低材料门槛饮品 |
| 奶香饼干杯 | 苏打饼干 + AD 钙奶 | 拌制 | 甜点与首次试吃留言 |
| 蛋炒饭 | 大米 + 鸡蛋 | 平底锅 | 正餐与饱腹恢复，教学配方 |
| 胡萝卜蛋饭 | 大米 + 鸡蛋 + 胡萝卜 | 平底锅 | 正餐与少量健康恢复 |
| 水果松饼 | 面粉 + 鸡蛋 + 苹果或香蕉 | 平底锅 | 水果口味变体与摆盘作品 |
| 奶香小饼干 | 面粉 + AD 钙奶 | 烤箱 | 烘焙入门 |
| 胡萝卜蛋饼 | 胡萝卜 + 鸡蛋 | 平底锅 | 两种材料的轻正餐 |
| 米香煎饼 | 大米 + 面粉 + 鸡蛋 | 平底锅 | 米饭点心与不同恢复搭配 |
| 水果蛋奶布丁 | 鸡蛋 + AD 钙奶 + 苹果或香蕉 | 烤箱 | 蛋奶甜点与口味收藏 |
| 苹果烤派 | 苹果 + 面粉 + AD 钙奶 | 烤箱 | 主题下午茶与摆盘 |

水果变体分别有成品图与表现，但按同一基础配方计数。“八味手册”仍使用表中前八道固定配方，完整食谱本显示全部十二道的发现情况。先用双果沙拉与蛋炒饭完成数据和界面，再扩齐其余内容。

配方定义至少包含稳定 ID、材料数量、可接受的水果变体、做法、成品 ID、恢复效果、解锁条件、首做经验、角色反馈与表现素材键。成品使用 `dish_` 前缀的内置物品 ID，加入正式物品注册；成品不在商店直接出售，首发也不提供卖出料理换金币。

## 三类短互动

玩耍页展示三种玩法、玩法记录和已解锁外观。默认适合手机单指操作，桌面支持鼠标和键盘。当前建议免费开始与重玩，金币用于永久玩具和外观选择，具体购买内容继续配置。

| 玩法 | 第一版操作 | 正常完成条件与记录 | 伙伴反馈 |
| --- | --- | --- | --- |
| 接球 | 伙伴抛球，玩家在时机范围内点击接回；提供降速辅助 | 一轮约 30-45 秒，走完有限次数的抛接即完成；记录连续接住次数与所用模式 | 抛出、接住、没接住、庆祝、鼓励 |
| 翻牌 | 默认 3×4 牌阵，点击配对，提供不限时辅助 | 找齐六对即完成；记录步数、用时与模式；后续开放更多牌面 | 观察、提示、找到一对、完成 |
| 吹泡泡 | 按住吹大、松开飘走，点击或让伙伴追着戳破 | 自由结束；奖励根据实际有效互动定义，候选为累计约 30 秒参与且完成数次吹泡泡；记录特殊形状 | 看泡泡、追逐、戳破、惊喜 |

最终局长和奖励条件在可玩版本中校准。基础奖励不要求达到高分；速度、正确率用于玩法记录和表现收藏。练习与正常完成的计数在模式配置中明确，不能仅通过进入页面、停在后台或立刻退出领取完成奖励。

基础翻牌直接开放；玩具球首次实际使用后永久开放接球，老档中已持有或有明确使用记录的玩家也可解锁；泡泡棒购买一次永久使用。泡泡棒放在玩法装备状态中，不反复消耗；旧玩具球原有恢复用途继续保留。

一局开始时记录参与角色、模式、随机局面和奖励依据。帧动画、指针坐标和粒子放在组件内，匹配结果、抛接轮数等关键进度才进入状态快照。切后台暂停主动游戏计时，返回可以继续或结束；伙伴改去日程、睡眠或状态不适合继续时明确结束或暂停，不在后台自动完成一局。完成时合并记录、奖励和记忆并清除活动标记，重复结束无第二份奖励。

## 必要状态与模块

先新增三个领域模块，不提前制作通用剧情编辑器或另一套存档系统：

| 拟新增位置 | 职责与主要函数 |
| --- | --- |
| `src/core/kitchen.ts` | 配方与厨具配置、默认与归一化状态、研究预览、`craftRecipe`、首做与试吃登记 |
| `src/core/miniGames.ts` | 玩法配置、默认与归一化状态、开始/暂停/继续/完成、记录和外观解锁 |
| `src/core/companionMemories.ts` | 记录有意义的共同经历、挑选相关回忆、推进少量固定后续 |
| `src/ui/KitchenPage.tsx`、`src/ui/app/useKitchenController.ts` | 厨房展示、选择和提交动作，调用核心规则 |
| `src/ui/PlayPage.tsx`、`src/ui/app/usePlayController.ts` | 玩法入口、局内生命周期与结果展示 |
| `src/ui/play/` 下的三个玩法组件 | 各自的输入、局面与动画，调用统一的本局开始和完成接口 |

`PetState` 增加 `kitchen`、`miniGames`、`companionMemories` 三个字段，各自有默认值和小版本号，建议保存下列必要信息：

- 厨房：已发现配方、厨具解锁、各配方制作次数与首做时间、各角色首次试吃记录、最近制作操作标识；合计进度尽量由这些数据计算。
- 短互动：永久玩法与外观解锁、各模式最佳记录和完成次数、当前局的稳定标识与可恢复进度、开始时的参与角色与奖励依据。
- 记忆：有意义事件的 ID、参与者、地点、相关配方或作品、选择与结果、发生及最近提及时间，以及当前可继续的小目标。重复经历合并计数，关键回忆与收藏保留。

新物品加入 `BuiltinItemId`、`shopItems`/`specialItems` 和图片映射。基础生食材标为不可直接使用，配方成品可喂食；厨具和永久玩具使用专门购买动作写入解锁状态。现有未知物品与 Mod 回退继续工作，配方研究首版只识别明确配置的食材。

新字段在 `createDefaultPet`、`normalizePet` 中补齐，旧档没有制作记录时从零开始。存档外层版本不因增加可选领域字段而随意重做；按现有备份格式导出导入，并在需要移动运行时间时接入 `gameClock`。已经发生的回忆日期和正在进行的活动计时分别处理。

## 基础记忆先做三条完整后续

| 起点 | 之后怎样提起 | 可自愿继续的事与成果 |
| --- | --- | --- |
| 第一次做出并喂食蛋炒饭 | 伙伴在厨房提起那顿饭 | 再做一道不同正餐，留下第一张菜单页 |
| 第一次完成水果松饼试吃 | 伙伴看见另一种水果时提起上次口味 | 尝试另一已配置变体，留下对照的食谱照片 |
| 接球刷新一次个人记录 | 下一次打开玩耍页时伙伴做出接球动作 | 再一起玩一局，无需打破纪录，留下共同练习的合影 |

这些是可配置的内容样例，使用当前伙伴的稳定角色 ID。首次试吃记录与回忆相互引用，不给同一事件重复发奖。对白只描述真实发生过的事，当前只主动展示一个可继续目标，允许以后再做。冒险版本再加入邻居和基地场景，不在此次先行版本里实现旅行入口。

## 收益与兼容

一年目标的每份料理、每局短互动基础 12 心继续作为预算候选，按开始时已有等级倍率计算，之后沿用现有成就和增益卡追加方式。厨房一次批量制作按实际份数逐份执行相同奖励规则；小游戏一局只执行一次。食谱首做经验、固定成就奖励与普通产心分别有明确归属，回看动画或回忆不再发奖。

成品喂食继续走现有食物效果与喜好处理，料理技能和奖杯加成只应用一次。首做料理经验复用料理技能的升级逻辑，但不增加日程完成次数或大师次数。

当前旧物品收集目标由商店列表派生，增加可用物品前要固定已发布的旧清单；“任务大师”的旧普通成就集合也保持原目标。厨房的六项候选成就可直接用制作与试吃记录派生，短互动的首次完成和成绩收藏随后配置，避免另外堆一套重复统计。

还需在玩法实现期间定下两个经济选择：厨房批量制作是否持续按份产心，以及短互动是否取消原每日奖励次数。它们不妨碍先完成配方、游戏和存档流程，但必须在最终收益配置与经济验收前收敛。现有日收入 500 心只是测算条件，不能直接承诺所有玩家 366 天满级；厨房先行阶段的成长也单独检查。

## 画面与资源安排

厨房采用制作台与伙伴场景，桌面在侧边展示食谱详情，手机将材料、数量和操作放在场景下方。玩耍进入独立场景，主要操作区避开系统返回与底部手势区域，重要反馈同时提供画面与文字。

按素材充足安排每道料理的成品、变体、研究提示和制作过程；三个角色分别配置递食材、制作、试吃、接球和泡泡反应，并准备厨具、卡背、球外观、合影等收藏。局内动作通过各玩法自己的资源表选择，首页状态继续使用现有 `PetStatus`/`RecentActivity` 表示生活状态。

存档保存作品参数和素材 ID，图片资源随应用或资源包管理，不把整张图片重复塞入每条回忆。只加载当前场景所需资源，手机性能按同时显示的内容控制。角色缺少某一动作时使用该角色的基础反馈图，未知 Mod 仍可以完成基础操作。

## 开发顺序与验收

| 步骤 | 交付内容 | 完成标志 |
| --- | --- | --- |
| 1. 数据与兼容 | 新状态、物品、配方配置及旧目标固定清单 | 旧档能正常读取，新料理能作为已知物品保存 |
| 2. 厨房完整流程 | 双果沙拉、蛋炒饭、研究预览、直接/批量制作、喂食与首做记录 | 买材料到看到试吃反馈完整可用 |
| 3. 第一种短互动 | 翻牌、开始与暂停恢复、结束结算及玩法记录 | 一局可以完成，反复结束不重复奖励 |
| 4. 扩齐内容 | 十二道配方、厨具、可选制作表现、接球和吹泡泡 | 三类互动和全部基础做法独立可用 |
| 5. 记忆与收藏 | 三条后续、厨房成就、食谱留言及玩法收藏 | 真实经历在以后出现对应反馈 |
| 6. 收益与端侧检查 | 定下重复收益规则，校准数值和手机布局 | 功能与成长节奏可供用户体验验收 |

第一轮实现从步骤 1 和 2 开始，以“购买鸡蛋和大米 -> 制作蛋炒饭 -> 喂食 -> 留下首次试吃记录”为最小完整交付。地图规划保持在文档中，不与这段实现混做。

实现后优先用数据检查关键情况：材料不足和无效组合不扣除、批量与逐份结算一致、正常喂食确实消费、重复提交不重复发奖、旧档与导入不丢新字段、角色切换不改写已发生的经历。小游戏再检查后台暂停和重复结束。复用现有检查风格，必要时集中到一个小的检查脚本，不逐配方复制测试。

完成变更后运行一次对应数据检查及 `npm.cmd run build`，再验收主要页面的手机/桌面交互与中文显示；只有数据难以确认的画面表现才额外做视觉检查。玩法实质验证完成后提醒提交 Git，本轮不提交。
